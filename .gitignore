# Claude Code
.claude/

# OS files
.DS_Store
Thumbs.db

# Node
node_modules/

# Build output
# Phase 1+ decision: bundles under dist/ are committed to git so Netlify
# can publish them without a build step. dist/ is otherwise ignored.
dist/*
!dist/larry-engine.js
!dist/larry-engine.js.map
!dist/larry-cookies.js
!dist/larry-cookies.js.map
coverage/

# Working files
temporary screenshots/

# Environment
.env
.env.local

# Real-roster snapshots used for manual verification.
# Contain personal team data; never commit.
tests/fixtures/users/
